# Fractal Studio Backend URL (C++ compute service)
NEXT_PUBLIC_BACKEND_URL=http://localhost:18080

# Never inject FSD_COMPUTE_SERVICE_KEY into a browser build.
# Production browsers call Platform /v1/*; only the Platform Worker holds the
# private Compute service credential.
